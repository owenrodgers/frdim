
Project:


Examples:
    cone_slices.rs
    conic_sections.rs
    hypershaphes.rs

src:
    fourshapes -> {conics.rs, hyperspheres.rs}
    rendering -> {render.rs}
    meshes -> {mesh.rs, triangle.rs}
    la -> {mat3x3.rs, mat4x4.rs, vec3f.rs}
    
    lib.rs

Tests:
    main.rs


fourshapes -> **
    conics: valid, is_valid needs work, compute_conic needs work
    hypersphere: good

rendering -> **
    render: render() needs to be a general functions, renderable object struct

meshes ->
    mesh: fileio, build triangle needs work
    triangle: better Triangle::new() ? 

la ->
    {mat3x3, mat4x4}: both solid, pretty light
    vec3f: solid, implement macro? vec3f![0.0, 1.0, 2.0]




